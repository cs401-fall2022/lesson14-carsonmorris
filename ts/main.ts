window.onload = loaded;

/**
 * Simple Function that will be run when the browser is finished loading.
 */
function loaded() {
    let message = "Hello world";
    console.log("Loaded main.ts:" + message);
}